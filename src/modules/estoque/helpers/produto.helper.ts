// import { CurrencyHelper } from '../../common/helpers/currency.helper';
// import { ProdutoComValores } from '../types/produto.type';
//
// export class ProdutoHelper {
//   static normalize(produto: ProdutoComValores) {
//     produto.valores.forEach(valor => {
//       const valorEmCents = CurrencyHelper.createCurrencyInstance(
//         valor.valorEmCents,
//       );
//       valor.reais = valorEmCents.dollars();
//       valor.centavos = valorEmCents.cents();
//     });
//   }
// }
